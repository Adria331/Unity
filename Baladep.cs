using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Baladep : MonoBehaviour {
	public GameObject bala;
	// Use this for initialization
	void Start () {
		Destroy (bala, 5);
	}
	
	// Update is called once per frame
	void Update () {
	}
}
